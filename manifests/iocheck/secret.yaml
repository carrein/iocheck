apiVersion: v1
kind: Secret
metadata:
  name: iocheck-admin
  namespace: iocheck
type: Opaque
stringData:
  # Demo only. In a real deployment this lives in a secret manager
  # (sealed-secrets, external-secrets, vault).
  ADMIN_TOKEN: "demo-admin-token"
